specVersion: "v1"
serviceId: "track-driver"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "track-driver-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
